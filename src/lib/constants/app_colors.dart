import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF6200EE); // Primary
  static const Color primaryVariant = Color(0xFF3700B3); // Primary Variant
  static const Color secondary = Color(0xFF03DAC6); // Secondary
  static const Color secondaryVariant = Color(0xFF018786); // Secondary Variant

  // Light theme colors
  static const Color lightBackground = Color(0xFFFFFFFF); // Background
  static const Color lightSurface = Color(0xFFFFFFFF); // Surface
  static const Color lightCardBackground = Color(0xFFFFFFFF);
  static const Color lightTableHeader = Color(0xFFF5F5F5);
  static const Color onPrimary = Color(0xFFFFFFFF); // On Primary
  static const Color onSecondary = Color(0xFF000000); // On Secondary
  static const Color onBackground = Color(0xFF000000); // On Background
  static const Color onSurface = Color(0xFF000000); // On Surface

  // Dark theme colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkCardBackground = Color(0xFF1E1E1E);
  static const Color darkTableHeader = Color(0xFF2C2C2C);

  // Status colors
  static const Color error = Color(0xFFB00020); // Error
  static const Color onError = Color(0xFFFFFFFF); // On Error
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
}
